# =============================================================================
# YouTube History Scraper - Environment Configuration
# =============================================================================
# Kopiere diese Datei zu ".env" und fülle die Werte aus.
# WICHTIG: .env darf NIEMALS ins Git-Repo committed werden!
#
# Verwendung:
#   cp .env.example .env
#   # Dann Werte anpassen
# =============================================================================

# --- Supabase Configuration ---
# Deine Supabase REST API URL (ohne /rest/v1)
SUPABASE_URL=http://148.230.71.150:8000

# Supabase Service Role Key (für Admin-Zugriff)
SUPABASE_SERVICE_KEY=your-service-role-key-here

# Supabase Tabellen-Name
SUPABASE_TABLE=youtube_urls

# --- Chrome Configuration ---
# Pfad zu Chrome.exe
CHROME_PATH=C:\Program Files\Google\Chrome\Application\chrome.exe

# Chrome User Data Directory (muss eingeloggt sein in YouTube)
CHROME_USER_DATA_DIR=C:\ChromeData\chromeprofile

# Chrome Remote Debugging Port
CHROME_DEBUG_PORT=9222

# Timeout für Chrome-Start (Sekunden)
CHROME_WAIT_TIMEOUT=15

# --- Scraping Configuration ---
# Bevorzugte Untertitel-Sprache (de, en, etc.)
DEFAULT_SUBTITLE_LANG=de

# Standard-Quelle für neue Einträge
DEFAULT_SOURCE=powershell-run

# Standard-Priorität
DEFAULT_PRIORITY=0

# --- Optional: API Keys für KI-Features (Legacy src/main.py) ---
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
